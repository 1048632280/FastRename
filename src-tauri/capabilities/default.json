{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permissions for FastRename.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
